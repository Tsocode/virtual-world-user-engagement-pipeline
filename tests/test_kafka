import unittest
from kafka_producer_consumer import produce

class TestKafkaProducer(unittest.TestCase):
    def test_produce_message(self):
        config = {
            'bootstrap.servers': 'your_confluent_cloud_bootstrap_server'
        }
        topic = 'test_topic'
        self.assertIsNone(produce(topic, config))
